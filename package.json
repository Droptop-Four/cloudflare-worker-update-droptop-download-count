{
  "name": "update-droptop-download-count",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.59.1"
  },
  "dependencies": {
    "realm-web": "^2.0.0",
    "toucan-js": "^4.0.0"
  }
}
